#include <cs50.h>
#include <stdio.h>

int main(void)
{
    do {
        int height = get_int ("Height: ");
    } while (n<1);
    for (int i=1, int j=1; i<=height, j<=height; i++, j++)
    {
        printf ("# #")
    }
}